{
  "name": "commandcode-proxy-v4",
  "version": "4.19.1",
  "description": "Local OpenAI Chat Completions & Anthropic Messages compatible proxy for Command Code AI. Chinese dashboard, hardened translation, retries, idle timeouts.",
  "main": "dist/index.js",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wjf1/commandcode-proxy.git"
  },
  "homepage": "https://github.com/wjf1/commandcode-proxy",
  "bugs": {
    "url": "https://github.com/wjf1/commandcode-proxy/issues"
  },
  "author": {
    "name": "wjf1",
    "url": "https://github.com/wjf1"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "build:exe": "npm run build && esbuild dist/index.js --bundle --platform=node --format=cjs --outfile=dist/bundle.cjs --external:fsevents",
    "build:win": "npm run build:exe && pkg dist/bundle.cjs --target node22-win-x64 --output dist/commandcode-proxy-v4.exe --compress GZip",
    "test": "vitest run",
    "pretest": "npm run build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test:coverage": "vitest run --coverage"
  },
  "pkg": {
    "assets": [
      "public/**/*"
    ],
    "outputPath": "dist"
  },
  "keywords": [
    "commandcode",
    "proxy",
    "openai",
    "anthropic",
    "api-gateway"
  ],
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.6.2",
    "@vitest/coverage-v8": "^5.0.1",
    "@yao-pkg/pkg": "^6.22.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.11.0",
    "tsx": "^4.23.15",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "fastify": "^5.12.3"
  }
}
